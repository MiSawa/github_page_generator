---

title: Tags
tags: []

---

{{#> outer}}
  {{#*inline 'content'~}}
    <h1>{{title}}</h1>
    <ul>
      {{#each (all_tags) as |tag|}}
        <li>{{link_to_tag tag}}</li>
      {{/each}}
    </ul>
  {{/inline}}
{{/outer}}
